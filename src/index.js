angular
  .module('app', ['ui.router', 'angularjs-gauge'])
  .run(function () {
    // var killStateWatch = $rootScope.$on('$stateChangeStart', function () {
    //   var landingUrl = 'http://' + $window.location.host + '/ProXR/halsema/info';
    //   $window.location.href = landingUrl;
    // });
	//
    // $scope.off('$destroy', function () {
    //    $interval.cancel(killRunner);
    // });

  });
